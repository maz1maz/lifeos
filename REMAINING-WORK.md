# کارهای باقی‌ماندهٔ پروژه «هِسته»

آخرین به‌روزرسانی: ۲۶ اوت ۲۰۲۶ / ۴ شهریور ۱۴۰۵

این فایل فقط شامل کارهای **باقی‌مانده** است. قابلیت‌های پایه‌ای مثل تقویم، روزانه، مالی پایه، تسک، پروژه، زمان، عادت، پرتفوی دستی، فوتبال دستی، اخبار دستی، فیلم/سریال دستی، Inbox و گزارش اولیه در MVP فعلی وجود دارند.

---

## اولویت ۱ — تکمیل تجربهٔ روزانه

### تسک و بهره‌وری
- [x] تولید خودکار نمونهٔ بعدی برای تسک‌های تکرارشونده
- [x] نمایش درختی و جمع‌شدن/بازشدن زیرتسک‌ها
- [x] درصد پیشرفت تسک اصلی براساس زیرتسک‌ها
- [x] تایمر زندهٔ شروع/توقف
- [x] Pomodoro با اعلان پایان جلسه
- [x] ویرایش ثبت زمان، نه فقط حذف
- [x] گزارش زمان به تفکیک پروژه
- [x] Time Blocking در تقویم روزانه
- [x] مرور هفتگی: بردها، کارهای عقب‌افتاده و اولویت هفتهٔ بعد

### حال، سلامت و عادت‌ها
- [x] ویرایش/حذف/آرشیو عادت‌ها
- [x] نمودار انجام عادت در هفته و ماه
- [x] ثبت ورزش با نوع و مدت
- [x] ثبت آب، وزن، دارو و مکمل
- [x] یادآوری ثبت مود، خواب و عادت‌ها
- [x] بخش «بهترین لحظه»، شکرگزاری و برنامهٔ فردا

---

## اولویت ۲ — مالی کامل

- [x] انتقال پول بین دو حساب، بدون اثر اشتباه روی هزینه/درآمد
- [x] ویرایش کامل حساب‌ها و آرشیو حساب
- [x] اتصال رسید یا عکس فاکتور به تراکنش
- [x] تراکنش تکرارشوندهٔ واقعی و ساخت خودکار رکورد ماه/هفتهٔ بعد
- [x] بودجهٔ کل ماه
- [x] نمودار دایره‌ای هزینه بر اساس دسته
- [x] مقایسهٔ ماه جاری با ماه قبل
- [x] فیلتر تراکنش براساس حساب، دسته، بازه و مبلغ
- [x] خروجی CSV
- [ ] خروجی Excel — *فعلاً به‌جای این، CSV با BOM (باز می‌شود در اکسل با فارسی درست) پیاده شده؛ چون پروژه عمداً هیچ وابستگی npm ندارد و ساخت .xlsx واقعی بدون کتابخانه دست‌ساز و پرخطاست. اگر فرمت باینری واقعی لازم شد، این مورد باز می‌ماند.*
- [x] ثبت پرداخت/دریافت هنگام تسویهٔ بدهی و طلب
- [x] یادآوری بدهی، طلب و سررسید اشتراک

---

## اولویت ۳ — پرتفوی و دارایی‌ها

پرتفوی از پایه بازسازی شد: به‌جای یک ردیف ثابت به‌ازای هر دارایی، حالا هر خرید/فروش/سود نقدی/کارمزد یک تراکنش جداست و موجودی هر نماد (تعداد، میانگین خرید، سود محقق/نامحقق) از روی همین تراکنش‌ها محاسبه می‌شود.

- [x] ویرایش کامل دارایی — چون موجودی از تراکنش‌ها ساخته می‌شود، «ویرایش دارایی» یعنی ویرایش/حذف هر تراکنش به‌صورت جداگانه (`/api/investments/tx/:id`)؛ همه چیز از فرم ثبت تراکنش هم قابل ویرایش است
- [x] ثبت خرید، فروش، کارمزد و سود نقدی به‌صورت تراکنش‌های جداگانه
- [x] محاسبهٔ میانگین خرید دقیق — با روش میانگین موزون هزینه (Average Cost)، شامل کارمزد خرید؛ فروش میانگین را عوض نمی‌کند، فقط سود/زیان محقق‌شده را ثبت می‌کند
- [x] تاریخچهٔ ارزش پرتفوی — هر بار که صفحهٔ پرتفوی باز می‌شود، ارزش همان روز ثبت/به‌روزرسانی می‌شود (`/api/portfolio/history`)
- [x] نمودار رشد ارزش خالص دارایی‌ها — نمودار خطی SVG دست‌ساز، جدا برای هر ارز (دلار/تومان)
- [x] دریافت قیمت زندهٔ کریپتو — از CoinGecko، **بدون نیاز به هیچ کلیدی** (رایگان و کاملاً فعال از همین حالا)؛ هر ۱۵ دقیقه خودکار هم به‌روز می‌شود
- [x] دریافت قیمت سهام آمریکا — از Twelve Data با `STOCK_API_KEY`
- [ ] دریافت قیمت سهام ایران — *عمداً پیاده نشد: منبع رایگان/رسمی معتبری برای قیمت آنی نماد‌های بورس تهران پیدا نشد؛ گزینه‌های موجود (مثل tsetmc.com) غیررسمی و شکننده‌اند و بدون تست واقعی پیاده‌سازی‌شان ریسک داشت. فعلاً برای طلا، سهام ایران و «سایر» فقط ثبت قیمت دستی هست، که همیشه در دسترس است*
- [x] هشدار قیمت و سود/زیان — هشدار قیمت بالا/پایین یا سود/زیان درصدی؛ در «اعلان‌ها»ی داشبورد نمایش داده می‌شود و در صورت وصل بودن تلگرام، با فاصلهٔ حداقل ۶ ساعت هم پیام می‌فرستد
- [ ] **نیازمند اقدام خود کاربر (فقط برای قیمت سهام آمریکا):** یک کلید رایگان از twelvedata.com بگیر و به‌عنوان `STOCK_API_KEY` در `.env` بگذار. کریپتو و ثبت دستی قیمت بدون هیچ کلیدی از همین الان کار می‌کنند

---

## اولویت ۴ — اتصال سرویس‌ها

### Telegram
- [x] ساخت بات تلگرام — *به‌جای Webhook (که نیازمند URL عمومی پایدار است و تونل Cloudflare هر بار عوض می‌شود)، با Long Polling (`getUpdates`) پیاده شد که هیچ URL ورودی لازم ندارد*
- [x] محدودسازی بات به Telegram User ID صاحب حساب — *هر پیام با شناسهٔ عددی فرستنده به حساب هِستهٔ متصل چک می‌شود؛ اگر وصل نباشد، بات راهنمایی می‌کند*
- [x] ثبت مالی، کار، مود، خواب، تسک و یادداشت با پیام — *با همان `parseLifeText` که در «ثبت هوشمند» استفاده می‌شود*
- [x] فرمان‌های `/امروز`، `/موجودی`، `/پرتفوی` و `/گزارش_ماه`
- [x] گزارش صبح و شب در تلگرام — *هر ۵ دقیقه چک می‌شود، ساعت ۸ و ۲۱ برای هر کاربر وصل‌شده یک‌بار در روز*
- [x] اعلان بودجه، سررسید، عادت و بازی فوتبال — *در همان گزارش صبح/شب گنجانده شده*
- [ ] **نیازمند اقدام خود کاربر:** ساخت بات در @BotFather و افزودن `TELEGRAM_BOT_TOKEN` به `.env`، و ثبت شناسهٔ عددی تلگرام (از @userinfobot) در بخش «اتصال‌ها»ی تنظیمات. بدون این کلید، بات اصلاً روشن نمی‌شود (کد آماده و با تست خودکار پوشش داده شده، اما بدون توکن واقعی، ارتباط زنده با تلگرام تست نشده است)

### Spotify و YouTube
- [x] دکمهٔ Sign In Spotify — در تنظیمات → اتصال‌ها
- [x] OAuth و نگهداری امن Refresh Token Spotify — الگوبرداری از فلوی OAuth گوگل که در ورود با گوگل جواب می‌دهد
- [x] دریافت Recently Played Spotify — دکمهٔ «پخش‌های اخیر» پس از اتصال
- [ ] پادکست‌های Spotify — *فعلاً فقط Recently Played عمومی خوانده می‌شود؛ تفکیک پادکست از موسیقی نیاز به اسکوپ و پردازش اضافه دارد*
- [x] دکمهٔ Sign In YouTube/Google با Scope موردنیاز (`youtube.readonly`)
- [x] انتخاب Playlist شخصی YouTube — فهرست پلی‌لیست‌ها پس از اتصال نمایش داده می‌شود
- [x] خواندن ویدیوهای Playlist — با کلیک روی هر پلی‌لیست
- [x] ذخیرهٔ فعالیت رسانه‌ای در صفحهٔ روز — از طریق `/api/media-log` (بک‌اند و تست کامل است؛ دکمهٔ «ذخیره در امروز» روی هر آیتم Spotify/YouTube هنوز به رابط کاربری اضافه نشده)
- [x] صفحهٔ اتصال‌ها و دکمهٔ قطع اتصال — بخش «اتصال‌ها» در تنظیمات، برای هر سه سرویس
- [x] اسپاتیفای وصل شد — `SPOTIFY_CLIENT_ID`/`SPOTIFY_CLIENT_SECRET`/`SPOTIFY_REDIRECT_URI` گرفته و روی `.env` و Cloudflare (`wrangler secret put`) تنظیم شدند؛ مسیر اتصال زنده تست شد (ریدایرکت واقعی به Spotify با پارامترهای درست تأیید شد). خودِ تبادل کد→توکن هنوز end-to-end تست نشده چون نیاز به تأیید واقعی کاربر روی صفحهٔ Spotify داره
- [ ] یوتیوب — *نیازمند اقدام خود کاربر: `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` از Google Cloud Console (OAuth client, نوع Web application) با Redirect URI برابر `https://pdmaz.hamidreza-mazlaghani.workers.dev/api/integrations/youtube/callback`؛ همون Client ID/Secret هم‌زمان «ورود با Google» رو هم فعال می‌کنه*

---

## اولویت ۵ — فیلم و سریال

- [x] اتصال TMDB — کلید واقعی گرفته شد و در `.env` تنظیم شد
- [x] جست‌وجوی خودکار عنوان — کادر جست‌وجو در بالای کتابخانهٔ فیلم/سریال؛ با یک کلیک روی نتیجه، آیتم کامل ساخته می‌شود
- [x] پوستر، خلاصه، ژانر، کارگردان و امتیاز TMDB — با یک کلیک، همه از TMDB می‌آیند (بازیگر فعلاً آورده نمی‌شود، فقط کارگردان)؛ با یک جست‌وجوی واقعی («Interstellar») تأیید شد که همه‌چیز درست می‌آید
- [x] ثبت پلتفرم تماشا، زبان و افراد همراه
- [x] تگ‌ها و بخش اسپویلر
- [x] پیشرفت فصل/قسمت سریال با نوار پیشرفت
- [x] آمار تماشا: ساعت، ژانر محبوب، کارگردان محبوب و امتیاز میانگین
- [x] نمایش موارد دیده‌شده در صفحهٔ روز و تقویم

---

## اولویت ۶ — فوتبال

- [ ] قراردادن `API_FOOTBALL_KEY` (یا `RAPIDAPI_KEY`) و فعال‌سازی همگام‌سازی واقعی — *نیازمند اقدام خود کاربر. حالا دو مسیر ممکن است: کلید مستقیم از api-football.com (`API_FOOTBALL_KEY`)، یا اشتراک API-FOOTBALL روی RapidAPI (`RAPIDAPI_KEY`، همون کلید مشترک زیر). سرور اول `API_FOOTBALL_KEY` را امتحان می‌کند و اگر نبود به `RAPIDAPI_KEY` برمی‌گردد — کد `/api/football/remote/fixtures` و `/api/football/remote/odds` هر دو مسیر را پشتیبانی می‌کنند*
- [ ] ذخیره و Match کردن خودکار بازی‌های API با تیم‌های محبوب — *وابسته به مورد بالا*
- [ ] جدول لیگ‌ها — *وابسته به کلید API*
- [ ] نتیجه زنده، گل‌ها و آمار مسابقه — *وابسته به کلید API*
- [x] نمایش بازارهای اصلی و ضرایب 1xBet در صورت دسترس‌بودن API — کد کامل است (`/api/football/remote/odds`)، فقط منتظر کلید
- [x] منبع دوم برای ضریب مستقیم از 1xBet (بدون واسطهٔ API-Football) — از RapidAPI (`1xbet-api.p.rapidapi.com`) با همون `RAPIDAPI_KEY`؛ زنجیرهٔ رشتهٔ ورزشی → لیگ → بازی → بازار در `/api/football/remote/1xbet/*` و در مودال فوتبال (بخش «۱xBet مستقیم»)؛ چون بدون کلید واقعی امکان دیدن فرمت پاسخ نبود، فعلاً خروجی خام JSON نمایش داده می‌شود، نه فرم پردازش‌شده
- [x] منبع سوم برای آمار تفصیلی‌تر بازی (SofaScore، از RapidAPI) — `/api/football/remote/sofascore/event(/stats|/incidents)` با همون `RAPIDAPI_KEY`؛ چون این API شناسهٔ رویداد مخصوص به خودش را دارد (نه هماهنگ با شناسه‌های API-Football)، فعلاً با وارد کردن دستی شناسهٔ رویداد کار می‌کند، نه جست‌وجوی خودکار بر اساس نام تیم
- [ ] **نیازمند اقدام خود کاربر:** برای فعال‌کردن ۱xBet مستقیم و SofaScore، از rapidapi.com یک اشتراک روی هر دو API («1xBet API» و «SportAPI»/sportapi7) بگیر و کلید مشترک RapidAPI را با نام `RAPIDAPI_KEY` در `.env` بگذار. همین یک کلید برای هر سه منبع (API-FOOTBALL از طریق RapidAPI، 1xBet مستقیم، SofaScore) کافی است. کد ساختاری تست شده (۴۰۰/۵۰۳/۴۰۱ بدون کرش)، ولی چون کلید واقعی نبود، پاسخ واقعی این سه API هرگز end-to-end تست نشد و فرمت دقیق داده‌ها هنوز تأیید نشده
- [x] مقایسهٔ پیش‌بینی شخصی با نتیجهٔ نهایی
- [x] گزارش دقت پیش‌بینی در لیگ/ماه/فصل
- [x] تحلیل هفتهٔ قبل براساس بازی‌های ثبت‌شده
- [x] یادآوری قبل از بازی و شروع مسابقه

---

## اولویت ۷ — اخبار

- [x] مدیریت منابع RSS — افزودن/ویرایش/حذف در بخش «منابع RSS» داخل مودال اخبار
- [x] افزودن RSS دلخواه — هر آدرس RSS یا Atom معتبری قابل افزودنه؛ بدون نیاز به هیچ کلید API (خودِ RSS یک فرمت باز است). با فید واقعی BBC فارسی تست شد و درست کار کرد
- [x] دریافت خودکار و زمان‌بندی‌شدهٔ خبرها — هر ۳۰ دقیقه یک‌بار همهٔ منابع فعالِ همهٔ کاربرها خودکار Sync می‌شوند (جدا از دکمهٔ «همگام‌سازی همه» که برای Sync فوری دستیه)
- [x] فیلتر دسته، منبع و خبرهای ذخیره‌شده
- [x] خلاصهٔ هفتگی اخبار — آمار قطعی (تعداد، دسته، منبع) همیشه کار می‌کند؛ یک پاراگراف روایی اضافه هم اگر `AI_PROVIDER_API_KEY` تنظیم باشد اضافه می‌شود
- [x] خلاصه‌سازی AI برای خبرهای طولانی — نیازمند `AI_PROVIDER_API_KEY`
- [x] ترجمهٔ خبرهای انگلیسی — همون کلید AI؛ ترجمهٔ عنوان و متن به فارسی
- [x] پیشنهاد خبر مرتبط براساس خبرهای ذخیره‌شده — کاملاً قطعی و بدون AI (اشتراک دسته + همپوشانی کلمات عنوان)، روی همهٔ خبرهای کاربر کار می‌کند نه فقط ذخیره‌شده‌ها
- [ ] **نیازمند اقدام خود کاربر (فقط برای خلاصه‌سازی/ترجمهٔ هوشمند):** همون `AI_PROVIDER_API_KEY` که در اولویت ۸ توضیح داده شد. بدون اون، مدیریت منابع، Sync، فیلتر و خبر مرتبط کاملاً کار می‌کنند

---

## اولویت ۸ — AI و گزارش هوشمند

- [x] اتصال یک مدل AI با API Key امن — از Anthropic Messages API استفاده شده (`AI_PROVIDER_API_KEY` + `AI_MODEL` اختیاری، پیش‌فرض `claude-sonnet-5`)؛ کلید فقط سمت سرور و در `.env` نگه‌داری می‌شود، هیچ‌وقت به فرانت‌اند فرستاده نمی‌شود
- [x] چت با داده‌های شخصی — `/api/ai/chat`، در تنظیمات → دستیار هوشمند؛ خلاصه‌ای واقعی از مالی این ماه، حال/خواب ۷ روز اخیر، تسک‌های پیش‌رو و استریک عادت‌ها به مدل داده می‌شود
- [x] پاسخ به پرسش‌هایی مثل «این ماه چرا بیشتر خرج کردم؟» — همان چت، چون دسته‌بندی هزینهٔ ماه جاری در دادهٔ ارسالی هست
- [x] تحلیل ارتباط خواب، مود، ورزش و کار — `/api/ai/correlations` کاملاً قطعی و بدون نیاز به AI است (ضریب همبستگی پیرسون واقعی، تست خودکار دارد)
- [x] فهم متن آزاد فارسی فراتر از الگوهای سادهٔ فعلی — وقتی regex فعلی چیزی پیدا نکند و `AI_PROVIDER_API_KEY` تنظیم باشد، متن برای استخراج ساخت‌یافته به مدل داده می‌شود (هم در «ثبت هوشمند» هم در بات تلگرام)
- [x] پیشنهاد دسته‌بندی خودکار تراکنش — `/api/ai/suggest-category`؛ اول با یک نقشهٔ کلیدواژهٔ فارسی (کاملاً رایگان و آفلاین) حدس می‌زند، اگر مطابقتی نبود و کلید AI موجود بود از مدل می‌پرسد؛ در فرم افزودن تراکنش هم وصل است (با خروج از فیلد عنوان، دسته پیشنهاد می‌شود)
- [ ] پیشنهاد دسته‌بندی خودکار Inbox — *فعلاً فقط برای تراکنش وصل شده؛ صندوق ورودی هنوز فقط به تسک/یادداشت تبدیل می‌شود، نه تراکنش، پس این مورد باز مانده*
- [x] گزارش هوشمند روزانه، هفتگی و ماهانه — `/api/ai/report?period=daily|weekly|monthly`
- [x] پیشنهاد اولویت‌های فردا بر اساس ددلاین و عادت‌ها — `/api/ai/tomorrow-priorities`؛ بخش لیست ددلاین‌های عقب‌افتاده/فردا و عادت‌های انجام‌نشده کاملاً قطعی و بدون AI کار می‌کند؛ اگر کلید AI موجود باشد، یک پاراگراف انگیزشی هم اضافه می‌شود
- [ ] **نیازمند اقدام خود کاربر:** گرفتن یک API Key از console.anthropic.com و افزودن آن به `.env` با نام `AI_PROVIDER_API_KEY`. بدون این کلید، بخش‌های تحلیل ارتباط و پیشنهاد اولویت فردا (بخش قطعی‌اش) کار می‌کنند، ولی چت، گزارش هوشمند، فهم متن پیشرفته و دستهٔ پیشنهادی AI-محور فعال نمی‌شوند و پیام ۵۰۳ روشن برمی‌گردانند. کد این بخش‌ها نوشته و تست ساختاری (۵۰۳/۴۰۰/۴۰۱ بدون کرش) شده، ولی چون هنوز کلید واقعی نیست، پاسخ واقعی مدل هرگز end-to-end تست نشده

---

## اولویت ۹ — ماژول‌های هنوز ساخته‌نشده

هر چهار زیربخش کامل ساخته، تست و به‌صورت زنده تأیید شدند (بخش «روابط و رشد» در نوار کناری).

### روابط و مخاطبان
- [x] مخاطبان مهم: خانواده، دوست، همکار و مشتری
- [x] تولدها، مناسبت‌ها و یادآوری پیگیری — تولد نزدیک و یادآوری پیگیری هر دو در «اعلان‌ها»ی داشبورد نمایش داده می‌شوند
- [x] ثبت آخرین تماس/ملاقات و یادداشت شخصی — هر بار ثبت تماس، «آخرین تماس» مخاطب هم خودکار به‌روز می‌شود

### یادگیری و دانش شخصی
- [x] کتاب، دوره، مقاله، ویدیو و پادکست
- [x] درصد پیشرفت کتاب/دوره — نوار پیشرفت زنده بر اساس صفحه/جلسهٔ فعلی از کل
- [x] یادداشت و خلاصهٔ یادگیری
- [x] بوکمارک و Read Later

### خرید، سفر و اسناد
- [x] لیست خرید
- [x] تبدیل خرید انجام‌شده به تراکنش — با ثبت قیمت واقعی، خودکار یک تراکنش هزینهٔ واقعی می‌سازد
- [x] سفر، رزرو، بودجه و چک‌لیست سفر — هزینهٔ هر سفر از تراکنش‌های واقعی متصل‌شده به همان سفر محاسبه می‌شود، نه عدد دستی
- [x] اسناد مهم، گارانتی، تاریخ انقضا و یادآوری تمدید — یادآوری انقضا در «اعلان‌ها»ی داشبورد
- [x] ضمیمه‌های فایل و عکس — *فعلاً محلی (همون الگوی رسید مالی)؛ روی نسخهٔ Cloudflare بعد از فعال‌سازی R2 (که هنوز خودت باید یک‌بار از داشبورد فعالش کنی) به‌صورت خودکار به R2 منتقل می‌شود، کد Worker از قبل آماده است*

### هدف و مرور زندگی
- [x] اهداف سالانه، ماهانه و هفتگی
- [x] اتصال هدف‌ها به مالی و عادت — پیشرفت هدف از روی داده‌های واقعی همون عادت یا همون دستهٔ هزینه محاسبه می‌شود؛ اتصال به یادگیری هم در بک‌اند آماده است (فقط فرم افزودن هدف در رابط کاربری فعلاً فقط عادت/مالی رو نشون می‌ده)
- [x] ثبت بردها و موفقیت‌ها
- [x] دفتر تصمیم: گزینه‌ها، دلیل، نتیجه و بازبینی — نتیجه هر وقت که آماده بود قابل ثبت است
- [x] مرور ماهانه و سالانه — آمار قطعی (هزینه، تسک، برد، میانگین حال) همیشه کار می‌کند؛ یک پاراگراف روایی هم اگر `AI_PROVIDER_API_KEY` تنظیم باشد اضافه می‌شود
- [x] «یک سال پیش امروز» — از روی روزنگار، تسک‌های انجام‌شده، تراکنش‌ها، فیلم‌های تماشاشده و بردهای همون روز در سال قبل

---

## اولویت ۱۰ — آماده‌سازی Production با Cloudflare

جزئیات کامل، دستورات دیپلوی و محدودیت‌های شناخته‌شده در [`cloudflare/README.md`](../cloudflare/README.md). حساب واقعی Cloudflareت (`hamidreza.mazlaghani@gmail.com`) برای این کار استفاده شد.

- [x] انتقال API فعلی Node.js به Cloudflare Worker — پورت کامل و تست‌شدهٔ هر ۹۵ مسیر در `cloudflare/worker.js` (تولیدشده با `cloudflare/port.js` از روی `server.js`، تا با هر تغییر آینده در `server.js` بشه دوباره ساختش)
- [x] ساخت Cloudflare D1 — دیتابیس واقعی `pdmaz-db` ساخته شد؛ به‌جای اسکیمای رابطه‌ایِ `cloudflare/schema.sql` (که با پروژهٔ فعلی هم‌خوان نبود)، از یک جدول ساده (`kv`) برای نگه‌داری کل دیتابیس به‌صورت JSON استفاده شد — این یک سادگی عمدیه، در `cloudflare/README.md` توضیح داده شده چرا
- [x] انتقال داده‌های JSON فعلی به D1 — حساب واقعی‌ات (بدون سشن‌های قدیمی) منتقل شد
- [ ] ساخت R2 Bucket برای رسید، عکس و فایل — *نیازمند اقدام خود کاربر: R2 باید یک‌بار از داخل داشبورد Cloudflare فعال بشه (API اجازهٔ این کار رو نمی‌ده)؛ بعدش من می‌سازمش. تا اون موقع آپلود رسید خطای روشن می‌ده، نه کرش*
- [ ] انتقال Secretها به Cloudflare Worker Secrets — *دستورات دقیق در `cloudflare/README.md`؛ اجرا نشده چون نیاز به کلیدهای واقعی و دسترسی تعاملی به ترمینال داره (`wrangler secret put` مقدار رو مخفی می‌پرسه)*
- [x] استقرار رابط روی Cloudflare — به‌جای Pages جدا، از قابلیت Static Assets خودِ Workers استفاده شد (همون `public/index.html`، بدون نیاز به پروژهٔ دوم)
- [x] اتصال دامنه یا زیردامنه — طبق تصمیم خودت، زیردامنهٔ رایگان `*.workers.dev` (نه دامنهٔ اختصاصی)
- [ ] HTTPS و تنظیم Redirect URIهای OAuth — HTTPS روی `workers.dev` خودکاره؛ Redirect URIهای گوگل/اسپاتیفای/یوتیوب باید بعد از اولین `deploy` (وقتی آدرس نهایی مشخص شد) در کنسول همون سرویس‌ها ثبت بشن
- [ ] Google Login واقعی — کد آماده است، وابسته به دو مورد بالا (deploy + ثبت Redirect URI)
- [ ] فراموشی رمز و بازیابی رمز — *ساخته نشده؛ نیاز به یک سرویس ارسال ایمیل داره که هنوز انتخاب نشده. یک محدودیت واقعی هم پیدا شد: چون Workers سقف تکرار PBKDF2 رو ۱۰۰٬۰۰۰ گذاشته (server.js از ۱۳۰٬۰۰۰ استفاده می‌کنه)، رمزهای هش‌شدهٔ قدیمی روی این دیپلوی جواب نمی‌دن — یعنی حساب واقعی‌ات هم برای اولین ورود به یک رمز جدید نیاز داره*
- [ ] تأیید ایمیل — وابسته به همون سرویس ایمیل بالا
- [ ] Rate Limit، مدیریت Session و لاگ امنیتی — Rate Limiting Rule آمادهٔ Cloudflare از داشبورد قابل فعال‌سازیه؛ لاگ امنیتی سفارشی هنوز نوشته نشده
- [ ] PWA کامل، Service Worker و حالت آفلاین — `manifest.webmanifest` از قبل موجوده؛ Service Worker هنوز نوشته نشده
- [ ] بکاپ خودکار و بازیابی اطلاعات — طرحش مشخصه (یک Cron دیگه که خروجی D1 رو دوره‌ای در R2 بنویسه) ولی هنوز پیاده نشده
- [ ] **بات تلگرام از Long Polling به Webhook تغییر کرد** (چون Worker نمی‌تونه حلقهٔ دائمی داشته باشه) — بعد از تنظیم `TELEGRAM_BOT_TOKEN`، یک بار باید `setWebhook` تلگرام صدا زده بشه (دستور در `cloudflare/README.md`)
- [x] **اپ الان واقعاً آنلاینه و کارش تأیید شده**: https://pdmaz.hamidreza-mazlaghani.workers.dev — سهمیهٔ D1 ریست شد و signup/login/session/تسک/داشبورد و متن فارسی همه به‌صورت زنده روی دیتابیس واقعی تست شدند. یک باگ واقعیِ جدی هم همینجا پیدا و رفع شد: تابع تولیدکنندهٔ کد Worker (`cloudflare/port.js`) به‌اشتباه `write(env,db)` می‌ساخت، در حالی که تابع `write` در Worker فقط یک پارامتر (`db`) می‌گیرد — یعنی هر بار ذخیره‌سازی، به‌جای دادهٔ واقعی، یک دامپ بی‌ربط از خودِ `env` را می‌نوشت. این باگ کپیِ داده‌های واقعی‌ات را که برای D1 منتقل کرده بودم پاک کرده بود (فایل واقعی `data/db.json` که سرور Node ازش استفاده می‌کنه اصلاً دست نخورد، چون کاملاً جدا از D1 ذخیره می‌شه). بعد از رفع باگ، داده‌ها دوباره منتقل شدند و حالا حساب واقعی‌ات دوباره در D1 هست
- [x] رمز عبور حساب واقعی روی این دیپلوی تنظیم و تأیید شد (ورود واقعی تست شد، ۲۰۰ موفق)
- [x] `TMDB_API_KEY` روی دیپلوی تنظیم شد (`wrangler secret put`)
- [x] کد فیلم/سریال TMDB روی Cloudflare دیپلوی و زنده تأیید شد — یک خطای احراز هویت گذرا در `wrangler` بین راه پیش اومد (ورود کش‌شده به چالش ربات Cloudflare خورد)، ولی با یک تلاش دوباره خودش برطرف شد؛ جست‌وجوی TMDB روی آدرس واقعی هم تست شد و درست کار کرد
- [ ] **نیازمند اقدام خود کاربر:** فعال‌سازی R2 در داشبورد، و اجرای `wrangler secret put` برای بقیهٔ کلیدهایی که داری (تلگرام، فوتبال، هوش مصنوعی و...). همهٔ دستورات دقیق در `cloudflare/README.md`

---

## وابستگی‌های خارجی موردنیاز

این موارد را نباید در کد یا چت قرار داد؛ فقط به‌صورت Secret در Cloudflare:

```text
API_FOOTBALL_KEY
TMDB_API_KEY
SPOTIFY_CLIENT_ID
SPOTIFY_CLIENT_SECRET
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
TELEGRAM_BOT_TOKEN
AI_PROVIDER_API_KEY
```
